import SwiftUI

@main
struct SwitchUIApp: App {
    var body: some Scene {
        WindowGroup {
            Text("SwitchUI + LocalJIT is Active")
                .font(.largeTitle)
        }
    }
}
